spring:
  application:
    name: api-gateway-local
  profiles:
    active: local

--- # spring boot actuator config
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
